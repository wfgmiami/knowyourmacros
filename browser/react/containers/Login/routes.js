import Login from 'containers/Login/containers/LoginForm';

/** @type {{ path: string, component: React.Component }} */
const routes = {
  path: 'login',
  component: Login
};

export default routes;
