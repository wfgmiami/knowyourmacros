import PublicMeals from 'containers/PublicMeals';

/** @type {{ path: string, component: React.Component }} */
const routes = {
  path: 'public-meals',
  component: PublicMeals
};

export default routes;
