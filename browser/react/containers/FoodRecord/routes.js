import FoodRecord from 'containers/FoodRecord';

/** @type {{ path: string, component: React.Component }} */
const routes = {
  path: 'food-record',
  component: FoodRecord
};

export default routes;
