import Serving from 'containers/Database/containers/Add/containers/Serving';

/** @type {{ path: string, component: React.Component }} */
const routes = {
  path: 'serving',
  component: Serving
};

export default routes;
