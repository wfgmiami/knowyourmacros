const createWithMeal = require('./createWithMeal');
const findByDate = require('./findByDate');
const findMicroByDate = require('./findMicroByDate');
const makeHistoricalArray = require('./makeHistoricalArray');

module.exports = {
  createWithMeal,
  findByDate,
  findMicroByDate,
  makeHistoricalArray
};
